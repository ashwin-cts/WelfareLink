@model WelfareLink.Models.WelfareProgram

@{
    ViewData["Title"] = "Edit Programme";
}

<div class="page-header">
    <div>
        <h1><i class="bi bi-pencil-square"></i> Edit Programme</h1>
        <p style="margin: 0; color: #64748b;">Update programme details, budget allocation, and eligibility.</p>
    </div>
    <a asp-action="Index" class="btn-modern btn-secondary-modern">
        <i class="bi bi-arrow-left"></i> Back to List
    </a>
</div>

<div class="content-card">
    <form asp-action="Edit" method="post" id="editProgramForm">
        @Html.AntiForgeryToken()
        <input type="hidden" asp-for="ProgramID" />

        <div class="row g-4">
            <div class="col-12">
                <div class="form-floating-modern">
                    <label asp-for="Title" class="form-label-modern">Programme Title</label>
                    <input asp-for="Title" type="text" class="form-control" placeholder="e.g., Housing Support" />
                    <span asp-validation-for="Title" class="text-danger"></span>
                </div>
            </div>

            <div class="col-12">
                <div class="form-floating-modern">
                    <label asp-for="Description" class="form-label-modern">Description &amp; Objectives</label>
                    <textarea asp-for="Description" class="form-control" rows="4" placeholder="Enter objectives and eligibility details..."></textarea>
                    <span asp-validation-for="Description" class="text-danger"></span>
                </div>
            </div>

            <div class="col-md-6">
                <div class="form-floating-modern">
                    <label asp-for="StartDate" class="form-label-modern">Start Date</label>
                    <input asp-for="StartDate" type="date" class="form-control" value="@Model.StartDate.ToString("yyyy-MM-dd")" />
                    <span asp-validation-for="StartDate" class="text-danger"></span>
                </div>
            </div>

            <div class="col-md-6">
                <div class="form-floating-modern">
                    <label asp-for="EndDate" class="form-label-modern">End Date</label>
                    <input asp-for="EndDate" type="date" class="form-control" value="@Model.EndDate.ToString("yyyy-MM-dd")" />
                    <span asp-validation-for="EndDate" class="text-danger"></span>
                </div>
            </div>

            <div class="col-md-6">
                <div class="form-floating-modern">
                    <label asp-for="Budget" class="form-label-modern">Budget Allocation (₹)</label>
                    <input asp-for="Budget" type="number" class="form-control" step="0.01" placeholder="0.00" />
                    <span asp-validation-for="Budget" class="text-danger"></span>
                </div>
            </div>

            <div class="col-md-6">
                <div class="form-floating-modern">
                    <label asp-for="MaxBenefitPerCitizen" class="form-label-modern">Max Benefit Allowed Per Citizen (₹)</label>
                    <input asp-for="MaxBenefitPerCitizen" type="number" class="form-control" step="0.01" placeholder="0.00" />
                    <span asp-validation-for="MaxBenefitPerCitizen" class="text-danger"></span>
                </div>
            </div>

            <div class="col-md-6">
                <div class="form-floating-modern">
                    <label asp-for="Status" class="form-label-modern">Programme Status</label>
                    <select asp-for="Status" class="form-select">
                        <option value="Active">Active</option>
                        <option value="Completed">Completed</option>
                        <option value="Suspended">Suspended</option>
                    </select>
                    <span asp-validation-for="Status" class="text-danger"></span>
                </div>
            </div>

            <div class="col-12">
                <div class="form-floating-modern">
                    <label class="form-label-modern">Eligible Gender</label>
                    <input type="hidden" id="EligibleGender" asp-for="EligibleGender" />
                    <div class="d-flex flex-wrap gap-3 mt-2">
                        <div class="form-check">
                            <input class="form-check-input gender-cb" type="checkbox" value="Anyone" id="gAnyone" />
                            <label class="form-check-label" for="gAnyone"><i class="bi bi-people text-success"></i> Anyone (No Restriction)</label>
                        </div>
                        <div class="form-check">
                            <input class="form-check-input gender-cb" type="checkbox" value="Male" id="gMale" />
                            <label class="form-check-label" for="gMale"><i class="bi bi-gender-male text-primary"></i> Male</label>
                        </div>
                        <div class="form-check">
                            <input class="form-check-input gender-cb" type="checkbox" value="Female" id="gFemale" />
                            <label class="form-check-label" for="gFemale"><i class="bi bi-gender-female text-danger"></i> Female</label>
                        </div>
                        <div class="form-check">
                            <input class="form-check-input gender-cb" type="checkbox" value="Other" id="gOther" />
                            <label class="form-check-label" for="gOther"><i class="bi bi-gender-ambiguous text-warning"></i> Other</label>
                        </div>
                    </div>
                    <small class="text-muted mt-1 d-block"><i class="bi bi-info-circle"></i> "Anyone" cannot be combined with specific genders.</small>
                </div>
            </div>

            <div class="col-12">
                <div class="form-floating-modern">
                    <label class="form-label-modern">Required Documents</label>
                    <input type="hidden" id="RequiredDocuments" asp-for="RequiredDocuments" />
                    <div class="d-flex flex-wrap gap-3 mt-2">
                        <div class="form-check">
                            <input class="form-check-input req-doc-cb" type="checkbox" value="None" id="rdNone" />
                            <label class="form-check-label" for="rdNone"><i class="bi bi-x-circle text-secondary"></i> No Document Required</label>
                        </div>
                        <div class="form-check">
                            <input class="form-check-input req-doc-cb" type="checkbox" value="ID Proof" id="rdId" />
                            <label class="form-check-label" for="rdId"><i class="bi bi-person-vcard text-primary"></i> ID Proof</label>
                        </div>
                        <div class="form-check">
                            <input class="form-check-input req-doc-cb" type="checkbox" value="Residence Proof" id="rdRes" />
                            <label class="form-check-label" for="rdRes"><i class="bi bi-house text-success"></i> Residence Proof</label>
                        </div>
                        <div class="form-check">
                            <input class="form-check-input req-doc-cb" type="checkbox" value="Income Certificate" id="rdInc" />
                            <label class="form-check-label" for="rdInc"><i class="bi bi-currency-rupee text-warning"></i> Income Certificate</label>
                        </div>
                        <div class="form-check">
                            <input class="form-check-input req-doc-cb" type="checkbox" value="Bank Statement" id="rdBank" />
                            <label class="form-check-label" for="rdBank"><i class="bi bi-bank text-info"></i> Bank Statement</label>
                        </div>
                    </div>
                    <small class="text-muted mt-1 d-block"><i class="bi bi-info-circle"></i> "No Document Required" cannot be combined with other selections.</small>
                </div>
            </div>
        </div>

        <div class="mt-4 pt-3" style="border-top: 1px solid #e2e8f0; display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 1rem;">
            <div>
                <a asp-action="Details" asp-route-id="@Model.ProgramID" class="btn-modern btn-info-modern">
                    <i class="bi bi-eye"></i> View Details
                </a>
            </div>
            <div class="action-buttons">
                <a asp-action="Index" class="btn-modern btn-secondary-modern">
                    <i class="bi bi-x-circle"></i> Cancel
                </a>
                <button type="submit" class="btn-modern btn-primary-modern">
                    <i class="bi bi-check-circle"></i> Update Programme
                </button>
            </div>
        </div>
    </form>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    <script>
        var anyoneCheckbox = document.getElementById('gAnyone');
        var specificGenderCbs = document.querySelectorAll('.gender-cb:not(#gAnyone)');

        anyoneCheckbox.addEventListener('change', function () {
            if (this.checked) specificGenderCbs.forEach(cb => cb.checked = false);
            syncGenderHidden();
        });
        specificGenderCbs.forEach(function (cb) {
            cb.addEventListener('change', function () {
                if (this.checked) anyoneCheckbox.checked = false;
                syncGenderHidden();
            });
        });

        function syncGenderHidden() {
            var checked = Array.from(document.querySelectorAll('.gender-cb:checked')).map(cb => cb.value);
            document.getElementById('EligibleGender').value = checked.length > 0 ? checked.join(',') : 'Anyone';
        }

        var noneCheckbox = document.getElementById('rdNone');
        var otherCheckboxes = document.querySelectorAll('.req-doc-cb:not(#rdNone)');

        noneCheckbox.addEventListener('change', function () {
            if (this.checked) otherCheckboxes.forEach(cb => cb.checked = false);
            syncHidden();
        });
        otherCheckboxes.forEach(function (cb) {
            cb.addEventListener('change', function () {
                if (this.checked) noneCheckbox.checked = false;
                syncHidden();
            });
        });

        function syncHidden() {
            var checked = Array.from(document.querySelectorAll('.req-doc-cb:checked')).map(cb => cb.value);
            document.getElementById('RequiredDocuments').value = checked.length > 0 ? checked.join(',') : 'None';
        }

        document.getElementById('editProgramForm').addEventListener('submit', function () {
            syncGenderHidden();
            syncHidden();
        });

        (function () {
            var genderVal = document.getElementById('EligibleGender').value || 'Anyone';
            var genderParts = genderVal.split(',').map(s => s.trim());
            document.querySelectorAll('.gender-cb').forEach(function (cb) {
                cb.checked = genderParts.includes(cb.value);
            });
            if (!document.querySelector('.gender-cb:checked')) {
                anyoneCheckbox.checked = true;
            }

            var docVal = document.getElementById('RequiredDocuments').value || 'None';
            var docParts = docVal.split(',').map(s => s.trim());
            document.querySelectorAll('.req-doc-cb').forEach(function (cb) {
                cb.checked = docParts.includes(cb.value);
            });
        })();
    </script>
}
